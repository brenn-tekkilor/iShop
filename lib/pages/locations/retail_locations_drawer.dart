// Import the Google Maps package
import 'package:flutter/material.dart';

class RetailLocationsDrawer extends StatefulWidget {
  RetailLocationsDrawer({Key key}) : super(key: key);
  @override
  _RetailLocationsDrawerState createState() => _RetailLocationsDrawerState();
}

class _RetailLocationsDrawerState extends State<RetailLocationsDrawer> {
  @override
  Widget build(BuildContext context) {
    var height = MediaQuery.of(context).size.height;
    var width = MediaQuery.of(context).size.width;
  }
}
